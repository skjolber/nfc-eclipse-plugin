<project name="Binary QR" basedir="." default="make">

    <property name="src.zxing.binaryqr.dir"     value="./../zxing/zxing-binaryqr/src"/>
	<property name="src.zxing.core.dir"     value="./../zxing/zxing-core/src"/>
	<property name="src.zxing.javase.dir"     value="./../zxing/zxing-javase/src"/>

    <property name="build.dir"   value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="jar.dir"     value="./lib"/>
    <property name="jar.filename"     value="binaryqr"/>

    <property name="src.dir"     value="${build.dir}/src"/>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${src.dir}"/>
    </target>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <mkdir dir="${src.dir}"/>

      	 <copy todir="${src.dir}">
      	 	<fileset dir="${src.zxing.binaryqr.dir}"/>
      	 	<fileset dir="${src.zxing.core.dir}"/>
      	 	<fileset dir="${src.zxing.javase.dir}"/>
      	 </copy>
    	
        <javac srcdir="${src.dir}" destdir="${classes.dir}" target="1.6" source="1.6"/>
       	 <copy todir="${classes.dir}">
       	 	<fileset dir="${src.dir}" excludes="**/*.java"/>
       	 </copy>
       	
    </target>

    <target name="jar" depends="compile" >
        <mkdir dir="${jar.dir}"/>
        <jar destfile="${jar.dir}/${jar.filename}.jar" basedir="${classes.dir}">
            <manifest>
            </manifest>
        </jar>
    </target>

    <target name="make" depends="jar,clean">
    </target>

</project>